# tg_bot_gigabash

Бот на Telegram с локальной LLM моделью **Qwen2.5-1.5B-Instruct-GGUF**.

## 📦 Установка библиотек

```bash
pip install telebot requests jsons python-dotenv
```

## ⚙ Настройка

Создайте файл `.env` в корне проекта и добавьте токен бота:

```
TELEGRAM_BOT_TOKEN=ваш_токен
```

## 🚀 Запуск

```bash
python main.py
```

## 🛠 Команды бота

* `/start` — список команд
* `/model` — показывает текущую модель
* `/clear` — очищает контекст текущего пользователя


- Запросы пользователя пересылаются LLM, запущеной на этом компьютере, и потом ответ пересылаются пользователю
- Любое сообщение отправляется в модель с учётом контекста ппрошлых сообщений. Контекст удаляется с помощь команды.
- Для кждого пользователя сохраняется свой контекст.
